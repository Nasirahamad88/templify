<!DOCTYPE html>
<html lang="en-US">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no"/>
    <meta name="theme-color" content="#7361DF"/>
    <title>Page with Reload Counter</title>
    <script>
        // Check if the counter exists in localStorage
        if (!localStorage.getItem('reloadCount')) {
            localStorage.setItem('reloadCount', 0);  // Initialize if not set
        }

        // Increment the counter
        let reloadCount = parseInt(localStorage.getItem('reloadCount')) + 1;
        localStorage.setItem('reloadCount', reloadCount);

        // Display the counter in the console or on the page
        document.addEventListener('DOMContentLoaded', function() {
            document.getElementById('counter').textContent = `Page reload count: ${reloadCount}`;
        });
    </script>
</head>
<body>
    <h1>Page Reload Counter</h1>
    <p id="counter">Page reload count: 0</p>
</body>
</html>
